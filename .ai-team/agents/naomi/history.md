# History — Naomi (Backend Dev)

## Project Context
- **Project:** Smart A11y Scanner — AI accessibility scanner for websites
- **Stack:** TypeScript, Node.js, Playwright, Azure DevOps API
- **User:** GalGoldi72 (ggoldshtein@microsoft.com)
- **Team:** Holden (Lead), Avasarala (PM), Naomi (Backend), Alex (Frontend), Amos (Tester), Drummer (A11y Expert), Bobbie (UI Expert)

## Learnings
- DFS exploration: `exploreState()` now immediately recurses into new states discovered via click before continuing with sibling elements at the same depth. The key is passing the breadcrumb stack through recursion — each `clickAndExplore` builds a `BreadcrumbEntry` from the state change type (url_change → "click", overlay_opened → "panel_opened", etc.) and appends it to the breadcrumb before recursing. This makes the traversal explicitly depth-first.
- Navigation breadcrumb is a `BreadcrumbEntry[]` stored on `ExplorationState.navigationPath` and formatted into `Finding.reproSteps` as human-readable strings. Format: "Navigate to {url}", "Click '{text}'", "Panel '{name}' opened", etc. Every finding from deep exploration gets repro steps automatically.
- Screenshots are taken at two levels: (1) full-page `stateScreenshot` on `ExplorationState` when a new state is first visited (`fullPage: true`), and (2) viewport-level screenshot on each `Finding` (`fullPage: false`). If analyzer's per-finding screenshot fails, the state screenshot is used as fallback. Base64 PNG, no disk writes.
- `BreadcrumbEntry` type lives in `src/scanner/types.ts` alongside `ExplorationState` and `Finding`. Fields: `action`, `elementText`, `url`.
- `PageAnalyzer.analyzeCurrentPage()` now accepts optional `reproSteps?: string[]` parameter. This is a non-breaking change — callers without repro steps still work (the parameter is optional). The method stamps repro steps onto every finding it produces.
- ADO Test Management REST API uses a separate base path (`_apis/testplan/`) from Work Items (`_apis/wit/`). Test case steps are stored as XML in the `Microsoft.VSTS.TCM.Steps` field — lightweight regex parsing is sufficient since the structure is predictable.
- The `x-ms-continuationtoken` header is how ADO paginates test plan/suite endpoints — different from the `$skip`/`$top` pattern used by Work Items batch API.
- Test case step text uses natural language, so action parsing is inherently heuristic. Navigate/click/type patterns cover ~80% of well-written test steps. Steps that don't match any pattern get tagged `unknown` — the hybrid scanner skips them gracefully rather than failing.
- The hybrid scanner's 5-phase pipeline (priority scan → guided nav → automated crawl → gap analysis → enriched bug filing) keeps each concern isolated. Phase ordering matters: manual URLs first gives them analysis priority before the crawler's page budget is consumed.
- Linking bugs to test cases uses ADO's `Microsoft.VSTS.Common.TestedBy-Forward` relation type. This appears in both the bug and test case UI, giving QA teams traceability without extra work.
- Gap analysis coverage score is `overlap / union` — simple but immediately useful for showing customers where their manual and automated testing diverge.
- Holden scaffolded `package.json`, `tsconfig.json`, and skeleton files across `src/types/`, `src/config/`, `src/detection/`, `src/reporting/`, `src/ado/bug-creator.ts` before I started. Many of those have import errors because the types they reference don't exist yet or were renamed — that's expected for scaffolding.
- Drummer's `src/rules/types.ts` defines `AccessibilityRule`, `Severity`, `WcagLevel`, `RuleCategory`, `AnalysisMode`, `WcagReference`, `RuleCatalog`. All my scanner types build on these.
- `tsconfig.json` uses `module: "Node16"` — all relative imports need `.js` extensions. Also needed to add `"DOM"` and `"DOM.Iterable"` to `lib` for Playwright `page.evaluate()` callbacks that run in browser context.
- Holden's `bug-creator.ts` expects `IADOClient`, `ADOWorkItem`, `ADOCreateResult` from my client. I exported those interfaces so it compiles.
- Holden's shared types (`src/types/scan-types.ts`, `src/types/finding.ts`, etc.) define a parallel type system for CLI/reporting. My scanner has its own internal types in `src/scanner/types.ts`. These will need reconciliation — my `ScanResult` should map to the shared `ScanResult` at the reporting boundary.
- Playwright `page.evaluate()` runs code in the browser. Color contrast can be computed inline using `window.getComputedStyle()` and the WCAG luminance formula. Works well for elements with explicit bg colors; inherited/layered backgrounds need a more sophisticated approach later.
- The scanner gracefully handles network failures — DNS resolution, timeouts, navigation errors all result in a `PageResult` with an `error` field, not a crash.
- POC verified: 6 rule checks fire correctly against a local test page (img-alt-text, form-input-label, heading-hierarchy, document-lang, link-name, button-name, color-contrast). 14 findings detected from a single test page.
- POC sprint: Added configurable scan timeout (default 600s/10min). `ScanConfig.timeout` sets the deadline; crawler checks it before each URL, engine checks before each page analysis. Partial results returned on timeout with `timedOut: true`.
- POC sprint: Replaced old `auth?: { username, password }` with rich `AuthConfig` supporting three flows: cookie injection (`context.addCookies`), form-based login (auto-detect username/email + password inputs, submit), and env-var credentials (`A11Y_SCANNER_CREDENTIALS=user:pass`). Customer creds, not service creds.
- POC sprint: `ScanResult` now includes `url`, `scanDate`, `duration`, `timedOut`, `pagesScanned`, and `summary.byWcagLevel`. Old fields (`durationMs`, `startedAt`) kept as `@deprecated` for backward compat.
- POC sprint: `Finding` now carries `pageUrl` and optional `screenshotPath` so each finding is self-contained for reporters. PageAnalyzer stamps `pageUrl` before returning.
- POC sprint: Added `scan(config)` as the primary public API on `ScanEngine` — accepts config at call time. `run()` kept as `@deprecated` wrapper so CLI and hybrid-scanner don't break.
- Login form detection uses a broad selector list (input[type=email], input[name*=user], etc.) and submit button heuristics (button[type=submit], button:has-text("Sign in"), etc.). Falls back to pressing Enter on the password field if no submit button found.
- `@axe-core/playwright` integrates cleanly via `new AxeBuilder({ page }).withTags([...]).analyze()`. The named export `{ AxeBuilder }` is required (not default import). Configuring `.withTags()` with WCAG 2.0/2.1/2.2 + best-practice gives 100+ rules automatically.
- Axe violations map to our Finding type via: `violation.id` → ruleId, `violation.impact` → severity (direct 1:1), tags containing `wcag###` patterns → wcagCriterion (parse digits into dotted notation like "1.1.1"), and `cat.*` tags → RuleCategory via a lookup table.
- Deduplication between hand-rolled and axe-core findings requires an equivalence map (e.g., our `document-lang` ↔ axe's `html-has-lang`, our `heading-hierarchy` ↔ axe's `heading-order`). Match on equivalent ruleId + exact selector match to avoid false positives.
- `page.accessibility.snapshot()` was removed in Playwright 1.42+. For ARIA role-based element discovery, query the DOM directly with `[role="..."]` selectors and check for accessible names via `aria-label`, `aria-labelledby`, or `textContent`.
- Axe-core works on `data:` URLs, which means deduplication actively replaces hand-rolled findings in tests. Tests must accept either the hand-rolled ruleId or the axe-core equivalent.
- `findInteractiveElements()` now classifies every element into 3 priority tiers before the DFS explores them. P1 (CONTENT_AREA) = elements inside `[role="main"]`/`main`, tabs, data rows, command bar actions, Fluent UI content patterns. P2 (NAVIGATION) = nav links, `.ms-Nav-link`, menu items in nav context. P3 (CHROME) = account/profile, collapse/expand nav, O365 header, panel close buttons — these are filtered out entirely and never clicked. Classification uses a combination of landmark containment (`isInsideMain`, `isInsideNav`, `isInsideHeader`), text/aria-label regex matching, and Fluent UI class patterns. The element data (containment booleans, className, ariaLabel) is gathered in `page.evaluate()` for efficiency — one round-trip to the browser, then classification happens in Node.
- The `InteractiveElement` interface was extended with `ariaLabel`, `isInsideMain`, `isInsideNav`, `isInsideHeader`, `className`, and `priority` fields to carry classification context from the browser to the Node-side classifier.
- Sorting by priority ensures the DFS always explores content elements first, nav elements second. Combined with the existing `MAX_ELEMENTS_PER_STATE` cap and deadline checking, this means the scanner spends its time budget on actual content rather than infrastructure chrome.
- GuidedExplorer executes ImportedTestScenario steps sequentially via Playwright. After each step, it runs PageAnalyzer.analyzeCurrentPage() for a11y findings, then optionally spawns a DeepExplorer for 1-level auto-exploration from the step's end state. Each step gets a 30s exploration cap to avoid blowing the deadline.
- Test plan parser (`test-plan-parser.ts`) supports three input formats: YAML file, JSON file, and inline CLI strings. All converge to `ImportedTestScenario[]` — the same type used by the ADO API importer. This means GuidedExplorer doesn't need to know the source.
- ADO test management URL parsing extracts orgUrl, project, planId, and optional suiteId from both `dev.azure.com` and `*.visualstudio.com` URL formats. The planId comes from `?planId=` query param, not the path — different from Work Item URLs where the ID is in the path.
- The `parseAction()` regex logic is duplicated between `test-case-importer.ts` and `test-plan-parser.ts`. This is intentional — the parser needs to be standalone (no ADO dependency), and extracting a shared module would couple the two. If patterns diverge, refactor later.
- GuidedExplorer's click strategy cascades: getByRole (button/link/tab/menuitem with accessible name) → getByText → CSS selector. This is more robust than hybrid-scanner's getByText → CSS fallback because role-based locators match even when visible text is truncated or has extra whitespace.
- The `TestPlanConfig` type on `ScanConfig` is the integration point: engine checks `this.config.testPlan` before SPA discovery. Source can be `ado-api`, `file`, or `inline`. The `loadTestPlanScenarios()` method dispatches to the right loader.
- `GuidedExplorationResult` is stored on `ScanResult.guidedResults` so Alex's reporters can render a step-by-step timeline. Each `GuidedStepResult` carries separate `findings` (from the step itself) and `explorationFindings` (from auto-explore after the step).

## 2026-02-23: Team Decisions Merged
📌 **Smart Element Prioritization** — New decision by Naomi: Elements now classified into P1 (Content), P2 (Navigation), P3 (Chrome). DFS explores content first. Integrated into deep-explorer.ts for immediate time-budget improvement.

📌 **GuidedExplorer & Test Plan Parser** — New decision by Naomi: New file guided-explorer.ts executes test plan steps sequentially, running PageAnalyzer + optional DeepExplorer auto-exploration after each step. File parser supports YAML/JSON/inline formats. Integrated into ScanEngine.execute() via conditional 	estPlan branch.

📌 **ADO Test Plan Integration Architecture** — New design by Holden: Five-phase pipeline (guided scan → auto-explore → analysis). Element prioritization critical (Part 1). Guided mode design (Part 2). CLI flags and report integration assigned to Alex and others for Phase 3+.

📌 **AI Test Plan Learning & Generation** — Post-phase design by Holden: LEARN → INVENT pipeline to synthesize new test scenarios from human test execution patterns. PatternExtractor and TestPlanGenerator modules. Gated behind --learn / --generate / --ai-generate flags. Foundation for later phases.

## 2026-02-24: LEARN → INVENT Pipeline Implementation (Phases 1, 2, 4)
- Created `src/scanner/patterns/types.ts` with full type definitions: LearnedPagePattern, LandmarkPattern, ElementGroupPattern, TestedElementRef, UntestedElementRef, HeadingNode, ContentRegionPattern, LearnedInteractionPattern, StateChangeType, LearnedNavigationFlow, NavigationTransition, LearnedCoverageMap, ElementTypeCoverage, LearnedPatterns, ExecutionSummary, GeneratedTestScenario, GenerationStrategy, PageSnapshot, A11yTreeNode, InteractiveElement, ExtractionConfig, GenerationConfig. File already existed as scaffold from Bobbie — I updated it to add ExtractionConfig and enrich InteractiveElement with tag/ariaLabel/landmark containment fields.
- Created `src/scanner/patterns/pattern-database.ts` — PatternDatabase class with save/loadLatest/loadHistory/merge/saveGeneratedPlans/loadGeneratedPlans methods. Uses fs/promises, writes to `.a11y-patterns/{hostname}/` directory. Merge logic unions page patterns by fingerprint, accumulates element groups by groupRole+containerSelector, merges interaction patterns by name, extends navigation transitions, combines coverage maps.
- Created `src/scanner/patterns/pattern-extractor.ts` — PatternExtractor with extract(), extractPagePatterns(), extractElementGroups(), extractInteractionPatterns(), extractNavigationFlow(), extractCoverageMap(). Groups snapshots by URL, builds heading trees, identifies element groups (tablist, toolbar, table, etc.) from a11y tree nodes, classifies tested vs untested elements. Uses label-based similarity (Jaccard on words) and role comparison — no LLM. Generates MD5-based fingerprints for page structure matching.
- Added snapshot capture to `guided-explorer.ts`: private `snapshots: PageSnapshot[]` field, `captureSnapshot(page, stepIndex)` method using `page.evaluate()` to walk DOM collecting interactive elements, landmarks, headings, and simplified a11y tree. Called in `executeStep()` after successful action when `config.learn` or `config.captureSnapshots` is truthy. Public `getSnapshots()` method exposed for engine.
- Wired LEARN → GENERATE into `engine.ts`: After guided exploration, if `config.learn` is true, runs PatternExtractor → PatternDatabase.save(). If `config.generate`, dynamically imports TestPlanGenerator, generates scenarios, saves plans, executes generated plans through GuidedExplorer round 2, tags findings with '[AI-Generated Test]'.
- Added to ScanConfig: learn, generate, aiGenerate, patternDir, maxGenerated, generationStrategies, captureSnapshots fields.
- Added to ScanResult: learningSummary and generationSummary fields (not yet populated in buildResult — will wire when end-to-end testing confirms the pipeline).
- `test-plan-generator.ts` was already implemented by Bobbie with all 4 heuristic strategies. No stub needed.
- Build passes (tsc), all 43 existing tests pass.
- Incomplete color-contrast findings (`color-contrast` and `color-contrast-enhanced`) from axe-core's `results.incomplete` are now filtered out in the `runAxeChecks()` loop. These are unconfirmed "[Needs review]" items where axe can't determine actual rendered colors (gradients, transparent backgrounds, CSS custom properties). Confirmed contrast violations from `results.violations` are still reported. This was a single `continue` guard at the top of the incomplete processing loop in `page-analyzer.ts`.
- Created `src/scanner/dynamic-analyzer.ts` — `DynamicAnalyzer` class with 10 Playwright-driven checks across 5 categories: (1) Zoom & Reflow: `checkZoomReflow()` (WCAG 1.4.4/1.4.10) sets `document.body.style.zoom = '200%'` and checks for horizontal scrollbar + clipped overflow:hidden elements; `checkTextSpacing()` (WCAG 1.4.12) injects a `<style>` tag with 0.12em letter-spacing, 0.16em word-spacing, 1.5 line-height, 2em paragraph margin and detects overflow. Both clean up after themselves. (2) Keyboard & Focus: `checkKeyboardNavigation()` (WCAG 2.1.1/2.1.2/2.4.7) presses Tab 30 times, collects focus history, detects keyboard traps (3+ consecutive same-selector) and missing focus indicators (outline/box-shadow check); `checkFocusOrder()` (WCAG 2.4.3) tabs through elements and compares vertical bounding-rect positions to detect major backward jumps. (3) Voice Access: `checkLabelInName()` (WCAG 2.5.3) compares visible text to aria-label/aria-labelledby for interactive elements; `checkTargetSize()` (WCAG 2.5.8) checks getBoundingClientRect() < 24px. (4) Screen Reader: `checkLandmarks()` (WCAG 1.3.1) checks for main/nav/any landmarks; `checkSkipLinks()` (WCAG 2.4.1) verifies first focusable element is a skip link; `checkLiveRegions()` (WCAG 4.1.3) heuristically detects toast/notification/alert containers missing aria-live. (5) Orientation: `checkOrientation()` (WCAG 1.3.4) tests portrait 768×1024 and landscape 1024×768, checks overflow and CSS orientation locks. Every check is wrapped in independent try/catch. A `deadline` parameter on `analyze()` skips remaining checks if time runs out. Added `dynamicChecks?: boolean` to `ScanConfig` in types.ts (default: not set = false). The engine will call `dynamicAnalyzer.analyze(page)` after `pageAnalyzer.analyze(page)` in a future integration step.
- Key design pattern: each dynamic check resets page state in a `finally` block — zoom reset, injected styles removed, viewport restored. This prevents one check from corrupting the page for subsequent checks.
- Focus indicator detection uses `getComputedStyle()` to check `outlineStyle !== 'none'` and `boxShadow !== 'none'`. This is a heuristic — it catches CSS outline/box-shadow indicators but misses background-color-only or border-only focus styles. Good enough for automated detection; edge cases need manual review.
- Keyboard trap detection is conservative: only fires when the exact same selector appears 3+ times consecutively. This avoids false positives from modal focus traps (which cycle through multiple elements) while catching genuine single-element traps.
- `checkLiveRegions()` uses class-name heuristics (`.toast`, `.notification`, `[class*="alert"]`, etc.) to find containers likely to update dynamically. This is inherently fuzzy — can't detect JS-driven content changes without actually observing mutations over time. A future enhancement could use MutationObserver during a wait period.
- `checkOrientation()` uses 768×1024 and 1024×768 per the task spec, which tests tablet-like viewports. Drummer's plan used mobile sizes (360×640). The tablet sizes are more appropriate for orientation testing since mobile already implies reflow concerns covered by zoom/reflow checks.
- **SIP URL normalization:** After SSO login, Microsoft redirects to `sip.security.microsoft.com` (backend hostname) instead of the user-facing `security.microsoft.com`. Added `normalizePageUrl()` exported from `page-analyzer.ts` — checks if the page URL hostname has a `sip.` prefix relative to the target URL hostname, and replaces it. Applied at all points where `finding.pageUrl` is set: `analyze()`, `analyzeCurrentPage()`, `runAxeChecks()` in PageAnalyzer, and `exploreState()`, `dismissInitialPopups()` in DeepExplorer. The target URL comes from `ScanConfig.url`.
- **Popup dismissal hardening:** The `closeOverlay()` method now verifies overlay count actually decreases after each strategy. Reordered strategies: Escape first (most reliable for Microsoft portals), then close button selectors, then text-based dismiss buttons, then click-outside. Added `tryTextBasedDismiss()` which searches for buttons with text like "Got it", "Close", "Dismiss", "OK", "Skip", "Not now" inside dialog/modal containers. `dismissInitialPopups()` now also falls back to `tryTextBasedDismiss()` if the overlay persists after `closeOverlay()`.
- **Finding deduplication:** Added `deduplicateFindings()` private method to `ScanEngine` in `engine.ts`. Runs after all pages are collected and before `buildResult()`. Dedup key is `ruleId + selector` (falls back to `ruleId + htmlSnippet` if selector is missing). Keeps the first occurrence per key — it has the best screenshot/context from initial discovery. Iterates pages in order, maintaining a global `Set<string>` of seen keys, and replaces each page's `findings` array with only unique entries. Logs `📊 Deduplicated: {original} → {unique} findings ({removed} duplicates removed)` when duplicates are found. This handles cross-page duplicates (same element found when revisiting the same page state via popups/navigation) and page-level findings like `meta-viewport` that fire identically on every visit.

📌 Team update (2026-03-03): Security audit completed — gitignore added, sensitive data removed, credentials validated. Security audit by Holden
